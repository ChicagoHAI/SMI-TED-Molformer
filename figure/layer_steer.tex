\begin{figure*}[t]
\centering
\begin{subfigure}[t]{0.24\textwidth}
    \centering
    \includegraphics[width=\linewidth]{results_steer_layers/by_layer_is_aromatic.pdf}
    \subcaption{is aromatic}
    \label{fig:steer-is-aromatic}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.24\textwidth}
    \centering
    \includegraphics[width=\linewidth]{results_steer_layers/by_layer_is_in_ring.pdf}
    \subcaption{is in ring}
    \label{fig:steer-is-in-ring}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.24\textwidth}
    \centering
    \includegraphics[width=\linewidth]{results_steer_layers/by_layer_degree.pdf}
    \subcaption{degree}
    \label{fig:steer-degree}
\end{subfigure}\hfill
\begin{subfigure}[t]{0.24\textwidth}
    \centering
    \includegraphics[width=\linewidth]{results_steer_layers/by_layer_chiral_tag.pdf}
    \subcaption{chiral tag}
    \label{fig:steer-chiral-tag}
\end{subfigure}

\vspace{0.4em}

\centerline{\includegraphics[width=0.45\textwidth]{results_steer_layers/by_layer_legend.pdf}}

\caption{\textbf{Intervention success rate as a function of the steer layer.} For each (task, model, layer~$\ell$) we intervene at $\ell$ along the layer-$\ell$ probe direction and read the layer-$12$ probe; the curve plots the best success rate over $\alpha$ on $200$ held-out QM9 molecules. Aromaticity (\ref{fig:steer-is-aromatic}) is at $1.00$ at every depth in both models. Ring membership (\ref{fig:steer-is-in-ring}), degree (\ref{fig:steer-degree}), and chiral tag (\ref{fig:steer-chiral-tag}) all show the cleanest pretraining-difference shape: \smi's success rises rapidly and saturates at $1.0$ by mid-layers, while \molm's climbs gradually and only reaches $1.00$ at the top of the encoder, even though \molm's probe accuracy on these tasks is roughly constant across mid-layers.}
\label{fig:steer-layer}
\end{figure*}
